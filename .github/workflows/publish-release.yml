name: Publish Release

on:
  release:
    types: [created]

permissions:
  contents: write

jobs:
  build:
    name: Build - ${{ matrix.platform.name }}
    runs-on: ${{ matrix.platform.os }}
    
    strategy:
      fail-fast: false
      matrix:
        platform:
          # Linux builds
          - name: Linux x86_64
            os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            artifact_name: gliror_linux_amd64
            use_cross: false
          
          - name: Linux ARM64
            os: ubuntu-latest
            target: aarch64-unknown-linux-gnu
            artifact_name: gliror_linux_arm64
            use_cross: true
          
          - name: Linux ARMv7
            os: ubuntu-latest
            target: armv7-unknown-linux-gnueabihf
            artifact_name: gliror_linux_armv7
            use_cross: true
          
          # macOS builds
          - name: macOS x86_64
            os: macos-latest
            target: x86_64-apple-darwin
            artifact_name: gliror_macos_amd64
            use_cross: false
          
          - name: macOS ARM64
            os: macos-latest
            target: aarch64-apple-darwin
            artifact_name: gliror_macos_arm64
            use_cross: false
          
          # Windows builds
          - name: Windows x86_64
            os: windows-latest
            target: x86_64-pc-windows-msvc
            artifact_name: gliror_windows_amd64
            use_cross: false
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Rust (for non-cross builds)
      if: matrix.platform.use_cross == false
      uses: dtolnay/rust-toolchain@stable
      with:
        targets: ${{ matrix.platform.target }}

    - name: Cache cargo registry (non-cross)
      if: matrix.platform.use_cross == false
      uses: actions/cache@v4
      with:
        path: ~/.cargo/registry
        key: ${{ runner.os }}-cargo-registry-${{ hashFiles('**/Cargo.lock') }}
        restore-keys: |
          ${{ runner.os }}-cargo-registry-

    - name: Cache cargo index (non-cross)
      if: matrix.platform.use_cross == false
      uses: actions/cache@v4
      with:
        path: ~/.cargo/git
        key: ${{ runner.os }}-cargo-git-${{ hashFiles('**/Cargo.lock') }}
        restore-keys: |
          ${{ runner.os }}-cargo-git-

    - name: Cache target directory (non-cross)
      if: matrix.platform.use_cross == false
      uses: actions/cache@v4
      with:
        path: target
        key: ${{ runner.os }}-${{ matrix.platform.target }}-cargo-target-${{ hashFiles('**/Cargo.lock') }}
        restore-keys: |
          ${{ runner.os }}-${{ matrix.platform.target }}-cargo-target-

    - name: Get version from Cargo.toml
      id: version
      shell: bash
      run: |
        VERSION=$(grep -m 1 "^version" Cargo.toml | cut -d '"' -f 2)
        echo "VERSION=$VERSION" >> $GITHUB_ENV
        echo "Version: $VERSION"

    - name: Build binary using cargo (non-cross)
      if: matrix.platform.use_cross == false
      shell: bash
      run: |
        cargo build --release --target ${{ matrix.platform.target }}

    - name: Build binary using actions-rust-cross (ARM Linux only)
      if: matrix.platform.use_cross == true
      uses: houseabsolute/actions-rust-cross@v0
      with:
        command: build
        target: ${{ matrix.platform.target }}
        args: "--locked --release"
        strip: true

    - name: Prepare release asset (Unix)
      if: runner.os != 'Windows'
      shell: bash
      run: |
        BINARY_NAME="gliror"
        BINARY_PATH="target/${{ matrix.platform.target }}/release/$BINARY_NAME"
        ASSET_NAME="${{ matrix.platform.artifact_name }}_v${{ env.VERSION }}"
        
        echo "Copying $BINARY_PATH to $ASSET_NAME"
        cp "$BINARY_PATH" "$ASSET_NAME"
        chmod +x "$ASSET_NAME"
        
        # Show binary info
        ls -lh "$ASSET_NAME"
        file "$ASSET_NAME" || echo "file command not available"
        
        echo "ASSET_NAME=$ASSET_NAME" >> $GITHUB_ENV

    - name: Prepare release asset (Windows)
      if: runner.os == 'Windows'
      shell: bash
      run: |
        BINARY_NAME="gliror.exe"
        BINARY_PATH="target/${{ matrix.platform.target }}/release/$BINARY_NAME"
        ASSET_NAME="${{ matrix.platform.artifact_name }}_v${{ env.VERSION }}.exe"
        
        echo "Copying $BINARY_PATH to $ASSET_NAME"
        cp "$BINARY_PATH" "$ASSET_NAME"
        
        # Show binary info
        ls -lh "$ASSET_NAME"
        
        echo "ASSET_NAME=$ASSET_NAME" >> $GITHUB_ENV

    - name: Upload release asset
      uses: softprops/action-gh-release@v1
      with:
        files: ${{ env.ASSET_NAME }}
      env:
        GITHUB_TOKEN: ${{ secrets.RELEASE_UPLOAD_TOKEN }}
